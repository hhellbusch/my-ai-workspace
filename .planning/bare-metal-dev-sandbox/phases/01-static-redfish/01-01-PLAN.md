---
phase: 01-static-redfish
plan: 01-01
type: execute
---

<objective>
Deliver a runnable per-developer **Tier G1** sandbox: static Redfish BMC on localhost,
minimal mockup fixtures, and an Ansible smoke test proving `community.general.redfish_info`
can query VirtualMedia against the emulator.

Output: `devops/bare-metal-dev-sandbox/` with README, start/stop scripts, fixtures, smoke playbook.
</objective>

<context>
@.planning/bare-metal-dev-sandbox/BRIEF.md
@.planning/bare-metal-dev-sandbox/ROADMAP.md
@devops/ansible/examples/004_validate_virtual_media_ejection/playbook_redfish.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold sandbox directory</name>
  <files>devops/bare-metal-dev-sandbox/README.md</files>
  <action>
    Create `devops/bare-metal-dev-sandbox/` with README documenting fidelity tiers,
    prerequisites (podman, ansible), quick start, and known emulation gaps.
  </action>
  <verify>README exists with quick-start section</verify>
  <done>Directory structure and README in place</done>
</task>

<task type="auto">
  <name>Task 2: Static Redfish mockup + podman launcher</name>
  <files>devops/bare-metal-dev-sandbox/scripts/start-static.sh</files>
  <action>
    Add DMTF-style static mockup tree under `fixtures/static-redfish/` (Managers, Systems, VirtualMedia).
    Add `scripts/start-static.sh` and `scripts/stop-static.sh` using `quay.io/metal3-io/sushy-tools`
    `sushy-static` bound to `127.0.0.1:8000`.
  </action>
  <verify>curl http://127.0.0.1:8000/redfish/v1/ returns 200</verify>
  <done>Static BMC container starts and serves mockups</done>
</task>

<task type="auto">
  <name>Task 3: Ansible smoke playbook</name>
  <files>devops/bare-metal-dev-sandbox/playbooks/smoke-redfish.yml</files>
  <action>
    Playbook queries VirtualMedia via `community.general.redfish_info` and asserts CD entry exists.
    Inventory defaults: bmc_host=127.0.0.1, bmc_port=8000, bmc_user=root, bmc_password=calvin (sushy defaults).
  </action>
  <verify>ansible-playbook smoke-redfish.yml exits 0 with sandbox running</verify>
  <done>End-to-end G1 smoke passes</done>
</task>

</tasks>

<verification>
- [ ] `./scripts/start-static.sh` brings up endpoint
- [ ] `curl -s http://127.0.0.1:8000/redfish/v1/Managers` returns JSON
- [ ] `ansible-playbook playbooks/smoke-redfish.yml` succeeds
- [ ] `./scripts/stop-static.sh` tears down container
</verification>

<success_criteria>
- Developer with podman + ansible can run smoke test in under 5 minutes (after image pull)
- README lists at least 3 emulation gaps vs real iDRAC10
</success_criteria>
