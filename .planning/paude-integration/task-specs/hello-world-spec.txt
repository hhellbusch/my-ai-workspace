Create a file at `docs/paude-hello-world.md` with the following content:

```markdown
# Hello from Paude

This file was created autonomously by a Claude agent running inside a Paude container.

**Date:** <today's date in YYYY-MM-DD format>
**Agent:** Claude (via Paude)
**Purpose:** Smoke test — verifies the full paude run pipeline is working end-to-end.

## What this proves

- The agent received its spec and executed it
- The agent can write files to the workspace
- The agent committed its work to git
- The claim verifier confirmed the expected output exists
```

Replace `<today's date in YYYY-MM-DD format>` with the actual current date.

After creating the file, commit it:
  git add docs/paude-hello-world.md
  git commit -m "test: paude hello-world smoke test"

Do not create any other files. Do not modify any existing files.
