# cluster-label-sync values
#
# AUTO-GENERATED by pipelines/github-actions/aggregate-cluster-config.sh
# DO NOT EDIT MANUALLY — edit clusters/<name>/cluster.yaml instead and re-run.
#
# Regenerate with:
#   bash pipelines/github-actions/aggregate-cluster-config.sh argo-examples/framework

policyNamespace: open-cluster-management-global-set
remediationAction: enforce

clusters:
  - name: example-prod-east-1
    managedClusterLabels:
      group.env: production
      group.ocp-version: "4.15"
      group.region: us-east
      group.network: ovn-kubernetes
      group.infra: baremetal
      app.enabled/cert-manager: "true"
      app.enabled/external-secrets: "true"
      app.enabled/baremetal-hosts: "true"
      app.enabled/nvidia-gpu-operator: "true"
      app.disabled/cluster-logging: "true"

  - name: example-nonprod-dev-1
    managedClusterLabels:
      group.env: non-production
      group.ocp-version: "4.15"
      group.region: us-east
      group.network: ovn-kubernetes
      app.enabled/cert-manager: "true"
      app.enabled/cluster-logging: "true"
