apiVersion: v2
name: example
description: |
  Lab example component for the ArgoCD Sync Settings Lab.
  Copy this entire folder to components/lab/<your-name> and use it as
  your starting point. Update appName and greeting in values.yaml.
type: application
version: 0.1.0
appVersion: "1.0.0"
