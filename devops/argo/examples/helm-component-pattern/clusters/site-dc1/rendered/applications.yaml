# Generated by scripts/render-clusters.sh — do not edit by hand.
# Cluster: site-dc1
# Groups:  all virt-enabled
# Re-render: ./scripts/render-clusters.sh site-dc1
#
# mustMergeOverwrite resolution order (lowest → highest priority):
#   component-all           (groups/all/values.yaml)
#   component-virt-enabled  (groups/virt-enabled/values.yaml)
#   component-site-dc1      (clusters/site-dc1/values.yaml)

---
# Source: cluster-apps/templates/application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: site-dc1-cert-manager
  namespace: openshift-gitops
  labels:
    managed-by: cluster-apps
    cluster: site-dc1
    app: cert-manager
    env: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/YOUR-ORG/YOUR-REPO.git
    targetRevision: main
    path: devops/argo/examples/helm-component-pattern/components/cert-manager
    helm:
      # Values are fully resolved here by mustMergeOverwrite before Argo CD
      # sees this Application.  There is no further valueFiles cascade at sync
      # time — what you see in this block is exactly what the component chart
      # receives.
      values: |
        channel: stable-v1
        installPlanApproval: Manual
        namespace: cert-manager
        resources:
          requests:
            cpu: 50m
            memory: 128Mi

  destination:
    server: https://api.site-dc1.example.com:6443
    namespace: cert-manager

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: site-dc1-kubevirt-hyperconverged
  namespace: openshift-gitops
  labels:
    managed-by: cluster-apps
    cluster: site-dc1
    app: kubevirt-hyperconverged
    env: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/YOUR-ORG/YOUR-REPO.git
    targetRevision: main
    path: devops/argo/examples/helm-component-pattern/components/kubevirt-hyperconverged
    helm:
      # Values are fully resolved here by mustMergeOverwrite before Argo CD
      # sees this Application.  There is no further valueFiles cascade at sync
      # time — what you see in this block is exactly what the component chart
      # receives.
      values: |
        channel: stable
        installPlanApproval: Automatic
        namespace: openshift-cnv

  destination:
    server: https://api.site-dc1.example.com:6443
    namespace: openshift-cnv

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: site-dc1-nmstate
  namespace: openshift-gitops
  labels:
    managed-by: cluster-apps
    cluster: site-dc1
    app: nmstate
    env: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/YOUR-ORG/YOUR-REPO.git
    targetRevision: main
    path: devops/argo/examples/helm-component-pattern/components/nmstate
    helm:
      # Values are fully resolved here by mustMergeOverwrite before Argo CD
      # sees this Application.  There is no further valueFiles cascade at sync
      # time — what you see in this block is exactly what the component chart
      # receives.
      values: |
        channel: stable-4.16
        installPlanApproval: Automatic
        namespace: openshift-nmstate

  destination:
    server: https://api.site-dc1.example.com:6443
    namespace: openshift-nmstate

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
