# Generated by scripts/render-clusters.sh — do not edit by hand.
# Cluster: site-edge-1
# Groups:  all edge-sno
# Re-render: ./scripts/render-clusters.sh site-edge-1
#
# mustMergeOverwrite resolution order (lowest → highest priority):
#   component-all       (groups/all/values.yaml)
#   component-edge-sno  (groups/edge-sno/values.yaml)
#   component-site-edge-1  (clusters/site-edge-1/values.yaml)

---
# Source: cluster-apps/templates/application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: site-edge-1-cert-manager
  namespace: openshift-gitops
  labels:
    managed-by: cluster-apps
    cluster: site-edge-1
    app: cert-manager
    env: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/YOUR-ORG/YOUR-REPO.git
    targetRevision: main
    path: devops/argo/examples/helm-component-pattern/components/cert-manager
    helm:
      # Values are fully resolved here by mustMergeOverwrite before Argo CD
      # sees this Application.  There is no further valueFiles cascade at sync
      # time — what you see in this block is exactly what the component chart
      # receives.
      values: |
        channel: stable-v1
        installPlanApproval: Automatic
        namespace: cert-manager
        resources:
          requests:
            cpu: 25m
            memory: 64Mi

  destination:
    server: https://api.site-edge-1.example.com:6443
    namespace: cert-manager

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: site-edge-1-nmstate
  namespace: openshift-gitops
  labels:
    managed-by: cluster-apps
    cluster: site-edge-1
    app: nmstate
    env: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/YOUR-ORG/YOUR-REPO.git
    targetRevision: main
    path: devops/argo/examples/helm-component-pattern/components/nmstate
    helm:
      # Values are fully resolved here by mustMergeOverwrite before Argo CD
      # sees this Application.  There is no further valueFiles cascade at sync
      # time — what you see in this block is exactly what the component chart
      # receives.
      values: |
        channel: stable-4.16
        installPlanApproval: Automatic
        namespace: openshift-nmstate

  destination:
    server: https://api.site-edge-1.example.com:6443
    namespace: openshift-nmstate

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
