# Cluster: site-edge-1
# Component overrides only.
#
# Cluster metadata lives in clusters.yaml.
# This file has no overrides — group defaults from component-all and
# component-edge-sno apply without modification.
#
# The file is kept as a placeholder.  When a cluster-specific override is
# needed, add it here under component-site-edge-1.apps.<appName>.

# (no overrides)
