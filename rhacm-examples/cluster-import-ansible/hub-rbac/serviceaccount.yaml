---
# ServiceAccount for automated cluster imports
# This account has minimal permissions to create ManagedClusters and read import secrets
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cluster-importer
  namespace: open-cluster-management
  labels:
    app: rhacm-automation
    purpose: cluster-import
